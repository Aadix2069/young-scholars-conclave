/**
 * Full paper submission is temporarily disabled site-wide (deadline, form,
 * nav entry, and every mention of it) while it's on hold. Flip back to
 * `true` to re-enable - the feature code itself is untouched, only gated
 * behind this flag.
 */
export const FULL_PAPER_SUBMISSION_ENABLED = false;
